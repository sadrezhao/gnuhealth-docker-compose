{
  "Name": "${ORTHANC_NAME}",
  "RemoteAccessAllowed": true,
  "PostgreSQL": {
    "EnableIndex": true,
    "EnableStorage": true,
    "Host": "orthanc-postgres",
    "Port": 5432,
    "Database": "${ORTHANC_DB}",
    "Username": "${ORTHANC_DB_USER}",
    "Password": "${ORTHANC_DB_PASSWORD}"
  }
}
